<?php

return [

    /*
    |--------------------------------------------------------------------------
    | テストログイン（DB不要）
    |--------------------------------------------------------------------------
    | true のとき、コードに埋め込んだテスト用アカウント（App\Support\TestAccounts）で
    | DBを使わずにログインできる。DBが無い／未接続のテスト環境で画面確認するための機能。
    |
    | ⚠ 本番公開前に .env で ECS_TEST_LOGIN=false にして必ず無効化すること
    |   （固定パスワードのため）。
    */
    'test_login' => env('ECS_TEST_LOGIN', true),

];
